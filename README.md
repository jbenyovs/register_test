# Register test

## Install and run

* clone the repo 
* ```npm install```
* ```light-server -s . -p 8000```

or

* ```light-server -s . -p 8080```

NOTE: multiple tests will display multiple results in ms

